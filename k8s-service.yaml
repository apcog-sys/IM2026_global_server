apiVersion: v1
kind: Service
metadata:
  name: information-mediator-gs-svc
  labels:
    app: information-mediator-gs
spec:
  type: LoadBalancer
  selector:
    app: information-mediator-gs
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9000
